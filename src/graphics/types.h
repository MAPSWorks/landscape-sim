#pragma once
#include <renderer/vlk/types.h>

namespace graphics {
// Types
// ...

// Structures
// Settings for the renderer
struct Settings {
    // Settings required by the renderer
    renderer::vlk::Settings renderer_settings;
    // Other graphics setting here ....
};
};